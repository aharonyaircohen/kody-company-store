---
description: Draft a Kody capability
argument-hint: <what should it do>
---

Draft a Kody capability that does the following: $ARGUMENTS.

Output a folder proposal for `.kody/capabilities/<slug>/` with `profile.json`
metadata (`action`, `capabilityKind`, `agent`, `readsFrom`, `writesTo`,
scripts/tools when needed) and a `capability.md` body with clear H1, `## Job`,
`## Output`, `## Allowed Commands`, and `## Restrictions`.

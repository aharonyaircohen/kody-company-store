---
description: Draft scheduled Kody agentResponsibility
argument-hint: <what should it do>
---

Draft a Kody agentResponsibility does following: $ARGUMENTS.

Output folder proposal for `.kody/agent-responsibilities/<slug>/` with `profile.json` metadata (action, agentAction when needed, every, agent, readsFrom/writesTo) and `agent-responsibility.md` body with clear H1, `## Job`, `## AgentAction` when relevant, `## Output`, `## Allowed Commands`, and `## Restrictions`. Keep implementation recipes in agentAction skills/scripts, not in agentResponsibility body.

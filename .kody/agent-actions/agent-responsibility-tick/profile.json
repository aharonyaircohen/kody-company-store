{
  "name": "agent-responsibility-tick",
  "role": "primitive",
  "describe": "One classifier tick for one agentResponsibility folder: read intent + state, decide and execute via gh, emit next state.",
  "kind": "oneshot",
  "inputs": [
    {
      "name": "agentResponsibility",
      "flag": "--agentResponsibility",
      "type": "string",
      "required": true,
      "describe": "AgentResponsibility slug — folder name under .kody/agent-responsibilities/."
    },
    {
      "name": "force",
      "flag": "--force",
      "type": "bool",
      "describe": "When true, this is an operator-forced run. All body rules (allowed commands, restrictions, state schema) still apply. Used for manual triggers from the dashboard's 'Run now' button."
    }
  ],
  "claudeCode": {
    "model": "inherit",
    "permissionMode": "default",
    "maxTurns": 100,
    "maxThinkingTokens": null,
    "systemPromptAppend": null,
    "enableSubmitTool": true,
    "tools": [
      "Bash",
      "Read",
      "mcp__kody-submit"
    ],
    "hooks": [],
    "skills": [],
    "commands": [],
    "subagents": [],
    "plugins": [],
    "mcpServers": []
  },
  "cliTools": [
    {
      "name": "gh",
      "install": {
        "required": true,
        "checkCommand": "command -v gh"
      },
      "verify": "gh auth status",
      "usage": "Use `gh` for all GitHub actions: `gh pr list ...` to enumerate candidate PRs, `gh pr comment <n> --body \"...\"` to issue a Kody command, `gh pr view <n> --json mergeable,statusCheckRollup,headRefOid` to inspect state, `gh api ...` for anything else. NEVER edit files in the working tree.",
      "allowedUses": [
        "pr",
        "api",
        "issue"
      ]
    }
  ],
  "inputArtifacts": [],
  "outputArtifacts": [],
  "scripts": {
    "preflight": [
      {
        "script": "loadJobFromFile",
        "with": {
          "jobsDir": ".kody/agent-responsibilities",
          "slugArg": "agentResponsibility"
        }
      },
      {
        "script": "composePrompt"
      }
    ],
    "postflight": [
      {
        "script": "parseJobStateFromAgentResult",
        "with": {
          "fenceLabel": "kody-job-next-state"
        }
      },
      {
        "script": "writeJobStateFile"
      },
      {
        "script": "appendCompanyActivity"
      }
    ]
  }
}

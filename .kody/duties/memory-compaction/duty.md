# Memory Compaction

Refresh the memory compaction proposal.

The report should show memory footprint, duplicate or large-memory candidates, task recommendation backlog, and safe next actions.

It must not edit `.kody/memory/` directly.


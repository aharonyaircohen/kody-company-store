# agent-responsibility-tick-scripted

Deterministic agentResponsibility tick: runs the slug's `tickScript:` (declared in the agentResponsibility profile), parses next-state from its stdout, persists. No agent.

# agent-ask

Ad-hoc one-shot: run an agent identity against an inline message + context (from a dashboard @agent mention). Stateless — no agentResponsibility folder, no state, no commit. Replies into the originating thread.

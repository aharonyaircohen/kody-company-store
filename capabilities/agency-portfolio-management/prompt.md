# Instructions

Use the `agency-portfolio-management` skill. Run only this capability's job;
the owning loop decides when it runs.

Finish with `DONE` and a short `PR_SUMMARY`, or `FAILED: <reason>`.

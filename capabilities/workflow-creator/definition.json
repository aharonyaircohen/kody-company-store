{
  "id": "workflow-creator",
  "action": "workflow-creator",
  "purpose": "Create one Kody workflow ordered-run model from a focused request.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "issue": {
        "type": "integer",
        "description": "GitHub issue number containing the focused model creation request."
      },
      "dry_run": {
        "type": "boolean",
        "description": "Validate the generated workflow without creating a state-repo branch or pull request."
      }
    },
    "required": [
      "issue"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "WORKFLOW_CREATOR_COMPLETED",
          "WORKFLOW_CREATOR_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [],
  "success": "workflow-creator completes and returns a valid canonical result",
  "failure": "workflow-creator cannot return a valid canonical result"
}

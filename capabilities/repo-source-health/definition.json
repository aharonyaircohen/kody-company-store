{
  "id": "repo-source-health",
  "action": "repo-source-health",
  "purpose": "Run the repository's configured source checks and publish one commit status for the Observer.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "REPO_SOURCE_HEALTH_CHECKED",
          "REPO_SOURCE_HEALTH_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [
    "commit-status"
  ],
  "permissions": [
    "gh",
    "git",
    "node",
    "corepack"
  ],
  "success": "repo-source-health completes and returns a valid canonical result",
  "failure": "repo-source-health cannot return a valid canonical result"
}

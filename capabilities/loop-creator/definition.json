{
  "id": "loop-creator",
  "action": "loop-creator",
  "purpose": "Create one Kody agent loop wakeup model from a focused request.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "issue": {
        "type": "integer",
        "description": "GitHub issue number containing the focused model creation request."
      }
    },
    "required": [
      "issue"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "LOOP_CREATOR_COMPLETED",
          "LOOP_CREATOR_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [],
  "success": "loop-creator completes and returns a valid canonical result",
  "failure": "loop-creator cannot return a valid canonical result"
}

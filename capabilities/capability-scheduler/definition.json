{
  "id": "capability-scheduler",
  "action": "capability-scheduler",
  "purpose": "Compatibility capability helper. Goals and loops own capability fan-out; this helper does not create schedule.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "capability": {
        "type": "string",
        "description": "Optional capability slug for legacy explicit runs. Scheduled fan-out belongs to goals and loops."
      }
    },
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    {
      "name": "gh",
      "install": {
        "required": true,
        "checkCommand": "command -v gh"
      },
      "verify": "gh auth status",
      "usage": "",
      "allowedUses": [
        "api",
        "issue",
        "pr"
      ]
    }
  ],
  "success": "capability-scheduler completes and returns a valid canonical result",
  "failure": "capability-scheduler cannot return a valid canonical result"
}

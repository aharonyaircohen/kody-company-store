{
  "id": "quality-watch",
  "action": "quality-watch",
  "purpose": "Coordinate security, coverage, and flaky-test quality signals.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "QUALITY_WATCH_COMPLETED",
          "QUALITY_WATCH_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    "gh"
  ],
  "success": "quality-watch completes and returns a valid canonical result",
  "failure": "quality-watch cannot return a valid canonical result"
}

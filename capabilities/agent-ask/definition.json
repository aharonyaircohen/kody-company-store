{
  "id": "agent-ask",
  "action": "agent-ask",
  "purpose": "Ad-hoc one-shot: run an agent identity against an inline message + context (from a dashboard @agent mention). Stateless — no capability folder, no state, no commit. Replies into the originating thread.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent": {
        "type": "string",
        "description": "Agent slug — basename (without .md) of the agent identity file under .kody/agents/."
      },
      "thread": {
        "type": "string",
        "description": "Where to post the reply: `discussion:<n>` (or a bare number) for a Discussion, or `issue:<n>` for an issue/PR. When set, the agent answers as a comment on that thread."
      },
      "message": {
        "type": "string",
        "description": "Fallback inline message when no triggering comment body is available (CLI/testing). In production the message is read verbatim from the dispatching comment body."
      }
    },
    "required": [
      "agent"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    "gh"
  ],
  "success": "agent-ask completes and returns a valid canonical result",
  "failure": "agent-ask cannot return a valid canonical result"
}

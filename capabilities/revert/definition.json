{
  "id": "revert",
  "action": "revert",
  "purpose": "Revert one or more commits on an existing PR branch via `git revert`. No agent — fully mechanical.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pr": {
        "type": "integer",
        "description": "GitHub PR number whose branch to revert commits on."
      },
      "shas": {
        "type": "string",
        "description": "One or more commit SHAs (whitespace-separated) to revert. Each must exist in the PR branch's history."
      }
    },
    "required": [
      "pr",
      "shas"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "REVERT_COMPLETED",
          "REVERT_FAILED"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    {
      "name": "git",
      "install": {
        "required": true,
        "checkCommand": "git --version"
      },
      "verify": "git --version",
      "usage": "Used by revert.sh to stage `git revert --no-commit` and by commitAndPush to land the revert.",
      "allowedUses": [
        "revert",
        "rev-parse",
        "merge-base",
        "log",
        "commit",
        "push",
        "rev-list"
      ]
    },
    {
      "name": "gh",
      "install": {
        "required": true,
        "checkCommand": "gh --version"
      },
      "verify": "gh --version",
      "usage": "Used by revertFlow to read PR metadata and post status / failure comments.",
      "allowedUses": [
        "pr",
        "api",
        "issue"
      ]
    }
  ],
  "success": "revert completes and returns a valid canonical result",
  "failure": "revert cannot return a valid canonical result"
}

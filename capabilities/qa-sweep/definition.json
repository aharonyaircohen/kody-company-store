{
  "id": "qa-sweep",
  "action": "qa-sweep",
  "purpose": "Run broad exploratory QA against the live app and summarize actionable findings.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "QA_SWEEP_COMPLETED",
          "QA_SWEEP_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    "gh"
  ],
  "success": "qa-sweep completes and returns a valid canonical result",
  "failure": "qa-sweep cannot return a valid canonical result"
}

{
  "id": "goal-scheduler",
  "action": "goal-scheduler",
  "purpose": "Scheduled: tick every active managed goal via goal-manager. Legacy/unmanaged goal files are skipped.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    {
      "name": "python3",
      "install": {
        "required": true,
        "checkCommand": "command -v python3"
      },
      "verify": "python3 --version",
      "usage": "scheduler.sh parses each managed goal todo file",
      "allowedUses": []
    }
  ],
  "success": "goal-scheduler completes and returns a valid canonical result",
  "failure": "goal-scheduler cannot return a valid canonical result"
}

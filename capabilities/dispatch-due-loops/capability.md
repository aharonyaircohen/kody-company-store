# Dispatch Due Loops

Reserve and dispatch each due AI Agency Loop through the shared trigger and
policy path.

This capability does not own business purpose, schedules, or workflow design.
It only invokes the generic Loop dispatcher.

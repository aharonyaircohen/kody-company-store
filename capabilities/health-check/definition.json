{
  "id": "health-check",
  "action": "health-check",
  "purpose": "Report Kody-assigned tasks that have not been updated within the expected window.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "HEALTH_CHECK_COMPLETED",
          "HEALTH_CHECK_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    "gh"
  ],
  "success": "health-check completes and returns a valid canonical result",
  "failure": "health-check cannot return a valid canonical result"
}

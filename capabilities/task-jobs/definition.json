{
  "id": "task-jobs",
  "action": "task-jobs",
  "purpose": "Runs the next pending implementation from a hidden task job plan on an issue.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "issue": {
        "type": "integer",
        "description": "GitHub issue number that carries the task job plan."
      }
    },
    "required": [
      "issue"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [],
  "success": "task-jobs completes and returns a valid canonical result",
  "failure": "task-jobs cannot return a valid canonical result"
}

{
  "id": "dev-ci-health",
  "action": "dev-ci-health",
  "purpose": "Watch the repo default branch CI and open or reuse one repair issue when the branch is red.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "DEV_CI_HEALTH_COMPLETED",
          "DEV_CI_HEALTH_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [
    "gh"
  ],
  "success": "dev-ci-health completes and returns a valid canonical result",
  "failure": "dev-ci-health cannot return a valid canonical result"
}

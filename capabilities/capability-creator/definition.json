{
  "id": "capability-creator",
  "action": "capability-creator",
  "purpose": "Create one complete single-responsibility Kody capability from an ability contract.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "issue": {
        "type": "integer",
        "description": "GitHub issue number containing the focused model creation request."
      }
    },
    "required": [
      "issue"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "CAPABILITY_CREATOR_COMPLETED",
          "CAPABILITY_CREATOR_FAILED",
          "AGENT_NOT_RUN"
        ]
      },
      "reason": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      },
      "data": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "additionalProperties": true
  },
  "effects": [],
  "permissions": [],
  "success": "capability-creator completes and returns a valid canonical result",
  "failure": "capability-creator cannot return a valid canonical result"
}

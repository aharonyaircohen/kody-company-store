# Job Gap Scan

Deterministic implementation. `tick.sh`, which proposes one missing high-ROI capability, updates `reports/job-gap-scan.md`, updates capability state, and commits when not in dry-run or no-commit mode.

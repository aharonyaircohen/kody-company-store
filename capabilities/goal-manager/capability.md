# goal-manager

One deterministic tick for a managed goal: read destination/evidence/capabilities/route from <statePath>/goals/instances/<id>/state.json, dispatch the capability for the first missing evidence, or mark the goal done. No agent.
